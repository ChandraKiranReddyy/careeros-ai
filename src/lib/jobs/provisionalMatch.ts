/**
 * @deprecated Phase 3 provisional scorer — replaced by lib/matching/engine.ts (Phase 4).
 * Kept as a thin re-export so older imports keep working.
 */
export { applyMatchEngine as applyProvisionalMatches, matchJob as provisionalMatch } from '../matching/engine'
